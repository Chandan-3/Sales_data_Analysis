-- What is the total sales?
SELECT 
    SUM(sales) AS Total_sales
FROM
    super_store;
-- How many records do we have in this dataset?
SELECT 
    COUNT(*) AS Total_Records
FROM
    super_store;

-- Which region generates the highest sales?
SELECT 
    region, SUM(sales) AS Highest_sales
FROM
    super_store
GROUP BY region
ORDER BY Highest_sales DESC
LIMIT 1;

-- Which category generates the highest sales?
SELECT 
    category, SUM(sales) AS Highest_selling
FROM
    super_store
GROUP BY category
ORDER BY Highest_selling DESC
LIMIT 1;

-- Which segment contribute the most of sales?
SELECT 
    segment, SUM(sales) AS Most_selling
FROM
    super_store
GROUP BY segment
ORDER BY Most_selling DESC
LIMIT 1;


-- Intermediate level
-- What are the top 5 sub-categories by sales?
SELECT 
    sub_category, SUM(sales) AS Total_sales
FROM
    super_store
GROUP BY sub_category
ORDER BY Total_sales DESC
LIMIT 5;

-- which sub-categories is generating highest sales but low profit?
SELECT 
    sub_category,
    SUM(sales) AS total_sales,
    SUM(profit) AS total_profit
FROM super_store
GROUP BY sub_category
ORDER BY total_sales DESC, total_profit ASC
limit 3;

-- Q2. Show only the first 10 records.
select * from super_store

limit 10 ; 

-- Q3. Show only Category, Sub_Category, Sales, and Profit.
select category, sub_category, sales, profit
from super_store;

-- Q2. Show only the first 10 records.
select * from super_store

limit 10 ; 

-- Q3. Show only Category, Sub_Category, Sales, and Profit.
select category, sub_category, sales, profit
from super_store;

select * from super_store;
-- Q4. Find all orders from the Consumer segment.
SELECT *
FROM super_store
WHERE Segment = 'Consumer'
LIMIT 10;

-- Find all orders from the West region.
SELECT 
    *
FROM
    super_store
WHERE
    region = 'west'
LIMIT 15;
-- Find the top 5 sub-categories with high total sales but low profit margin.
use store;
SELECT 
    sub_category,
    SUM(sales) AS total_sales,
    SUM(profit) AS total_profit
FROM super_store
GROUP BY sub_category
ORDER BY total_sales DESC, total_profit ASC
limit 5;

-- Which states generate the highest profit?
select state,
 sum(profit) as total_profit
from super_store
group by state
limit 5;
-- Which sub-categories are making a loss?

use store;
select
    sub_category,
    ROUND(SUM(sales), 2) AS total_sales,
    ROUND(SUM(profit), 2) AS total_profit
FROM super_store
GROUP BY sub_category
HAVING SUM(profit) < 0
ORDER BY total_profit ASC;

-- Which sub-categories have high sales but total profit is negative?

SELECT 
    sub_category,
    ROUND(SUM(sales), 2) AS total_sales,
    ROUND(SUM(profit), 2) AS total_profit
FROM super_store
GROUP BY sub_category
HAVING SUM(sales) > 10000 
   AND SUM(profit) < 0
ORDER BY total_sales DESC;

-- Which category gives the highest average discount?

SELECT 
    category,
    ROUND(AVG(discount), 2) AS average_discount
FROM super_store
GROUP BY category
ORDER BY average_discount DESC;

-- Which customer segment performs best in sales and profit?

SELECT 
    segment,
    ROUND(SUM(sales), 2) AS total_sales,
    ROUND(SUM(profit), 2) AS total_profit,
    ROUND((SUM(profit) / SUM(sales)) * 100, 2) AS profit_margin_percent
FROM super_store
GROUP BY segment
ORDER BY total_sales DESC;

-- Which cities generate high sales but low profit?

SELECT 
    city,
    ROUND(SUM(sales), 2) AS total_sales,
    ROUND(SUM(profit), 2) AS total_profit,
    ROUND((SUM(profit) / SUM(sales)) * 100, 2) AS profit_margin_percent
FROM super_store
GROUP BY city
HAVING SUM(sales) > 5000
ORDER BY profit_margin_percent ASC
LIMIT 10;

-- Which shipping mode generates the highest sales and profit?

SELECT 
    ship_mode,
    COUNT(*) AS total_orders,
    ROUND(SUM(sales), 2) AS total_sales,
    ROUND(SUM(profit), 2) AS total_profit
FROM super_store
GROUP BY ship_mode
ORDER BY total_sales DESC;

DESCRIBE super_store;

-- What are the company’s overall sales, profit, quantity, average discount, and profit margin?

SELECT
    ROUND(SUM(sales), 2) AS total_sales,
    ROUND(SUM(profit), 2) AS total_profit,
    SUM(quantity) AS total_quantity,
    ROUND(AVG(discount), 2) AS avg_discount,
    ROUND((SUM(profit) / NULLIF(SUM(sales), 0)) * 100, 2) AS profit_margin_percent,
    COUNT(*) AS total_records
FROM super_store;

-- Which region performs best in sales and profitability?

SELECT
    region,
    ROUND(SUM(sales), 2) AS total_sales,
    ROUND(SUM(profit), 2) AS total_profit,
    ROUND((SUM(profit) / NULLIF(SUM(sales), 0)) * 100, 2) AS profit_margin_percent,
    SUM(quantity) AS total_quantity
FROM super_store
GROUP BY region
ORDER BY total_sales DESC;

-- Which sub-categories are causing losses?

SELECT
    sub_category,
    ROUND(SUM(sales), 2) AS total_sales,
    ROUND(SUM(profit), 2) AS total_profit,
    ROUND((SUM(profit) / NULLIF(SUM(sales), 0)) * 100, 2) AS profit_margin_percent
FROM super_store
GROUP BY sub_category
HAVING SUM(profit) < 0
ORDER BY total_profit ASC;

-- How does discount level affect profit?

SELECT
    CASE
        WHEN discount = 0 THEN 'No Discount'
        WHEN discount > 0 AND discount <= 0.10 THEN 'Low Discount'
        WHEN discount > 0.10 AND discount <= 0.30 THEN 'Medium Discount'
        ELSE 'High Discount'
    END AS discount_group,
    COUNT(*) AS total_records,
    ROUND(SUM(sales), 2) AS total_sales,
    ROUND(SUM(profit), 2) AS total_profit,
    ROUND(AVG(profit), 2) AS avg_profit_per_record,
    ROUND((SUM(profit) / NULLIF(SUM(sales), 0)) * 100, 2) AS profit_margin_percent
FROM super_store
GROUP BY discount_group
ORDER BY profit_margin_percent ASC;

-- Which states generate strong sales but still lose money?

SELECT
    state,
    ROUND(SUM(sales), 2) AS total_sales,
    ROUND(SUM(profit), 2) AS total_profit,
    ROUND((SUM(profit) / NULLIF(SUM(sales), 0)) * 100, 2) AS profit_margin_percent
FROM super_store
GROUP BY state
HAVING SUM(sales) > 10000
   AND SUM(profit) < 0
ORDER BY total_sales DESC;


-- What are the best-performing sub-categories inside each category?

SELECT
    category,
    sub_category,
    total_sales,
    total_profit,
    profit_margin_percent,
    sales_rank
FROM (
    SELECT
        category,
        sub_category,
        ROUND(SUM(sales), 2) AS total_sales,
        ROUND(SUM(profit), 2) AS total_profit,
        ROUND((SUM(profit) / NULLIF(SUM(sales), 0)) * 100, 2) AS profit_margin_percent,
        RANK() OVER (
            PARTITION BY category 
            ORDER BY SUM(sales) DESC
        ) AS sales_rank
    FROM super_store
    GROUP BY category, sub_category
) ranked_data
WHERE sales_rank <= 3
ORDER BY category, sales_rank;

SELECT *
FROM super_store
WHERE category IN ('Furniture', 'Technology') and city Like 'Chicago%';
